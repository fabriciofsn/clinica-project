import { IPaciente } from "@modules/domain/paciente/paciente.interface";
import { Document, Model } from "mongoose";

export class fromMongoToPaciente{
  public static fromMongoEntityToPaciente(entity: Document<[{}]>){
     
  }
}